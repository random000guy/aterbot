{
  "client": {
    "host": "localhost",
    "port": 25565,
    "username": "AFKBot"
  },
  "action": {
    "retryDelay": 5000,
    "messageInterval": 10000,  // Time in milliseconds to send a message
    "disconnectInterval": 20000,  // Time in milliseconds to disconnect the bot
    "holdDuration": 5000,
    "commands": ["forward", "back", "left", "right"]
  }
}
